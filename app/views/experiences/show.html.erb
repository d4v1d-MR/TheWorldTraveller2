
 <img class="img-rounded" style="margin-top: 10px;
    border-radius: 16px;
    width: 100%;
    height: 500px;"<%= image_tag @experience.image.url %>

  <div class="textoExperiencia">
  <h1 style="text-align: center"><%= @experience.title %></h1>

  <h3 style="text-align: center"><%= @experience.summary %></h3>

  <p><%= @experience.body %></p>

  </div>
  <h3 style="text-align: center">Comentarios:</h3>

  <div>
    <% @experience.comments.each do |comment| %>

   <ul id="comments">
    <li>
    <%= comment.name %> <b>dijo: </b>
    </li>
    <li>
      <%= comment.body %>
    </li>
   <% if user_signed_in? %>
    <% if current_user.email == comment.name %>
   <%= link_to 'Delete', [comment.experience, comment],
    method: :delete,
    data: {confirm: 'Are you sure?' }%>
   <%end%>
   <%end%>
    </ul>
    <% end %>
   </div>

  <% if user_signed_in? %>
  <div>
  <h3 style="text-align: center">Añadir un comentario: </h3>

   <%= render file:'comments/new' %>

  </div>
    <%end%>

<!--<%= link_to 'Back', experiences_path %>-->
